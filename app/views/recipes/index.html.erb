	<!-- Header -->
	<%= render 'header' %>

	<!-- Tag Gallery -->
	<%= render 'gallery' %>

	<!-- Families -->
	<%= render 'family' %>

	<!-- Recipe List -->
	<%= render 'list' %>

	<!-- Top Rated -->
	<%= render 'rated' %>

	<!-- Feedback -->
	<%= render 'feedback' %>

	<!-- Contact -->
	<%= render 'contact' %>

	<!-- Footer -->
	<%= render 'footer' %>
